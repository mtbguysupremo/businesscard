/* photo, name, buttons! etc */

import React from "react";

export default function About() {
    return (
        <div>
            <h3>I like to write programs and APPS, this puppy was made in React!</h3>
        </div>
        )
}